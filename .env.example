# MCP文档转换服务器环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置

# === 基础配置 ===
# MCP服务器名称
MCP_SERVER_NAME=DocumentConverter

# 日志级别 (DEBUG, INFO, WARNING, ERROR)
MCP_LOG_LEVEL=INFO

# 最大文件大小 (支持单位: B, KB, MB, GB)
MCP_MAX_FILE_SIZE=50MB

# 临时目录路径
MCP_TEMP_DIR=/tmp

# === 功能开关 ===
# 启用Pandoc转换 (true/false)
MCP_ENABLE_PANDOC=true

# 启用HTTP健康检查服务器 (true/false)
MCP_ENABLE_HTTP=true

# HTTP服务器端口
MCP_HTTP_PORT=8000

# === 云端部署配置 ===
# 云端平台类型 (modelscope, aliyun, aws, etc.)
CLOUD_PLATFORM=modelscope

# 容器内存限制
CONTAINER_MEMORY=512Mi

# 容器CPU限制
CONTAINER_CPU=500m

# === 安全配置 ===
# API密钥 (如果需要)
# MCP_API_KEY=your_api_key_here

# 允许的文件类型 (逗号分隔)
ALLOWED_FILE_TYPES=pdf,docx,doc,md,html,txt,rtf,odt

# 禁止的文件类型 (逗号分隔)
FORBIDDEN_FILE_TYPES=exe,bat,sh,ps1

# === 性能配置 ===
# 最大并发转换任务数
MAX_CONCURRENT_TASKS=5

# 任务超时时间 (秒)
TASK_TIMEOUT=300

# === 存储配置 ===
# 输出文件保留时间 (小时)
OUTPUT_RETENTION_HOURS=24

# 自动清理临时文件 (true/false)
AUTO_CLEANUP=true